#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t ;
    cin >> t;
    while (t--) {
        int n , x ;
        cin >> n >> x ;
        vector<int> fuel(n) ;
        for(int i =  0 ; i < n ; i++) {
            cin >> fuel[i] ;
        }
    }
    return 0;
}